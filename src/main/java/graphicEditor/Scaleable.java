package graphicEditor;

public interface Scaleable {

    /**
    * Resizes the shape to the specified percentage
    * Params: percentScale - percentage to resize the shape
    */
    void scale(int percentScale);
}
